% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db_info.R, R/timescale.R
\name{get_chunks_info}
\alias{get_chunks_info}
\title{Get chunks information for a hypertable}
\usage{
get_chunks_info(table_name, conn = NULL, debug = FALSE)

get_chunks_info(table_name, conn = NULL, debug = FALSE)
}
\arguments{
\item{table_name}{Name of the hypertable}

\item{conn}{Database connection object (optional, uses global connection if NULL)}

\item{debug}{Enable debug messages (optional, default FALSE)}
}
\value{
Data frame with chunk information

Data frame with chunk information
}
\description{
Retrieves information about chunks in a TimescaleDB hypertable,
including chunk size, compression status, and time ranges.
}
\examples{
\dontrun{
# Initialize connection
init_connection(
  host = "localhost", 
  port = 5432,
  db = "testdb", 
  user = "testuser", 
  pass = "testpass"
)

# Get chunks info
chunks <- get_chunks_info("sensor_data")
print(chunks)

# Or with explicit connection
conn <- get_connection()
chunks <- get_chunks_info("sensor_data", conn)
}
\dontrun{
# Initialize connection
init_connection(
  host = "localhost", 
  port = 5432,
  db = "testdb", 
  user = "testuser", 
  pass = "testpass"
)

# Get chunks info with required arguments first
chunks_info <- get_chunks_info("sensor_data")
print(chunks_info)

# With explicit connection as optional parameter
conn <- get_connection()
chunks_info <- get_chunks_info("sensor_data", conn)
}
}
