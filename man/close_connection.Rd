% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{close_connection}
\alias{close_connection}
\title{Close the global database connection}
\usage{
close_connection(clear_config = FALSE, debug = FALSE)
}
\arguments{
\item{clear_config}{Whether to also clear the stored connection configuration (default: FALSE)}

\item{debug}{Enable verbose debug output}
}
\value{
TRUE if successful
}
\description{
Closes the global database connection initialized with init_connection()
and removes it from the package environment.
}
\examples{
\dontrun{
# Initialize global connection
init_connection(host = "localhost", port = 5432, db = "testdb", 
                user = "testuser", pass = "testpass")
                
# Use connection through other functions
result <- get_query("SELECT * FROM my_table")

# Close connection when done
close_connection()
}
}
