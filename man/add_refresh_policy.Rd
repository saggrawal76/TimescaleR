% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/continuous_aggregates.R
\name{add_refresh_policy}
\alias{add_refresh_policy}
\title{Add a refresh policy to a continuous aggregate}
\usage{
add_refresh_policy(
  conn = NULL,
  view_name,
  refresh_interval,
  start_offset = NULL,
  end_offset = NULL,
  debug = FALSE
)
}
\arguments{
\item{conn}{Database connection object (optional, uses global connection if NULL)}

\item{view_name}{The name of the continuous aggregate view}

\item{refresh_interval}{The interval at which the view should be refreshed (e.g., '1 hour', '1 day')}

\item{start_offset}{Optional time interval specifying how far back to refresh (default NULL)}

\item{end_offset}{Optional time interval specifying how recent the data should be (default NULL)}

\item{debug}{Enable debug messages (optional, default FALSE)}
}
\value{
TRUE if the policy was added successfully
}
\description{
Adds an automatic refresh policy to a continuous aggregate view. This schedules
regular updates to keep the view in sync with the underlying data.
}
\examples{
\dontrun{
# Initialize connection
init_connection(
  host = "localhost", 
  port = 5432, 
  db = "testdb", 
  user = "testuser", 
  pass = "testpass", 
  schema = "public"
)

# Using global connection with explicit connection object
conn <- get_connection()
add_refresh_policy(conn, "metrics_hourly", "1 hour")

# Or with custom offsets
add_refresh_policy(
  conn,
  "metrics_daily", 
  "1 day",
  start_offset = "7 days",
  end_offset = "1 hour"
)
}
}
