% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tables.R
\name{copy_table}
\alias{copy_table}
\title{Copy a Table}
\usage{
copy_table(
  source_table,
  target_table,
  conn = NULL,
  source_schema = NULL,
  target_schema = NULL,
  with_data = TRUE,
  as_hypertable = TRUE,
  time_column = NULL,
  chunk_time_interval = "1 day",
  if_not_exists = FALSE,
  debug = FALSE
)
}
\arguments{
\item{source_table}{Name of the source table to copy}

\item{target_table}{Name of the target table to create}

\item{conn}{Database connection object (optional if global connection is initialized)}

\item{source_schema}{Schema for source table (optional)}

\item{target_schema}{Schema for target table (optional, defaults to source_schema if provided)}

\item{with_data}{Whether to copy the data (default: TRUE)}

\item{as_hypertable}{If TRUE and source is a hypertable, convert target to hypertable}

\item{time_column}{Time column name for hypertable conversion (required if as_hypertable is TRUE)}

\item{chunk_time_interval}{Interval for chunking (e.g., "1 day")}

\item{if_not_exists}{Whether to skip if hypertable already exists (default: FALSE)}

\item{debug}{Enable debug messages (optional, default FALSE)}
}
\value{
TRUE if successful
}
\description{
Creates a copy of a table with optional data cloning.
}
\examples{
\dontrun{
# Copy a table using global connection
init_connection(
  host = "localhost", 
  port = 5432,
  db = "testdb", 
  user = "testuser", 
  pass = "testpass"
)
copy_table("source_table", "target_table", with_data = TRUE)
close_connection()

# Or with explicit connection
conn <- connect_to_db(config)
copy_table("source_table", "target_table", conn, with_data = FALSE)
disconnect_from_db(conn)
}
}
