% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db_info.R, R/timescale.R
\name{decompress_chunks}
\alias{decompress_chunks}
\title{Decompress chunks for a hypertable}
\usage{
decompress_chunks(table_name, older_than, conn = NULL, debug = FALSE)

decompress_chunks(table_name, older_than, conn = NULL, debug = FALSE)
}
\arguments{
\item{table_name}{Name of the hypertable}

\item{older_than}{Time interval for chunks to decompress}

\item{conn}{Database connection object (optional, uses global connection if NULL)}

\item{debug}{Enable debug messages (optional, default FALSE)}

\item{newer_than}{Time interval to decompress chunks newer than this (optional)}
}
\value{
Number of chunks decompressed

TRUE if successful
}
\description{
Manually decompress chunks for a hypertable.
}
\examples{
\dontrun{
# Initialize connection
init_connection(
  host = "localhost", 
  port = 5432,
  db = "testdb", 
  user = "testuser", 
  pass = "testpass"
)

# Decompress all compressed chunks
decompressed <- decompress_chunks("sensor_data")

# Decompress chunks newer than 1 day
decompressed <- decompress_chunks("sensor_data", "1 day")

# Or with explicit connection
conn <- get_connection()
decompressed <- decompress_chunks("sensor_data", conn = conn)
}
\dontrun{
# Initialize connection
init_connection(
  host = "localhost", 
  port = 5432, 
  db = "testdb", 
  user = "testuser", 
  pass = "testpass", 
  schema = "public"
)
# Using global connection with required arguments first
decompress_chunks("sensor_data", "7 days")

# With explicit connection as optional parameter
conn <- get_connection()
decompress_chunks("sensor_data", "7 days", conn)
}
}
