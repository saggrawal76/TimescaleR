% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tables.R
\name{rename_table}
\alias{rename_table}
\title{Rename a Table}
\usage{
rename_table(old_name, new_name, conn = NULL, schema = NULL)
}
\arguments{
\item{old_name}{Current name of the table}

\item{new_name}{New name for the table}

\item{conn}{Database connection object (optional if global connection is initialized)}

\item{schema}{Schema name (optional)}
}
\value{
TRUE if successful
}
\description{
Changes the name of an existing table in the database.
}
\examples{
\dontrun{
# Rename a table using global connection
init_connection(
  host = "localhost", 
  port = 5432,
  db = "testdb", 
  user = "testuser", 
  pass = "testpass"
)
rename_table("old_table_name", "new_table_name")
close_connection()

# Or with explicit connection
conn <- connect_to_db(config)
rename_table("old_table_name", "new_table_name", conn)
disconnect_from_db(conn)
}
}
