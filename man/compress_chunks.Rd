% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db_info.R, R/timescale.R
\name{compress_chunks}
\alias{compress_chunks}
\title{Compress chunks for a hypertable}
\usage{
compress_chunks(table_name, older_than, conn = NULL, debug = FALSE)

compress_chunks(table_name, older_than, conn = NULL, debug = FALSE)
}
\arguments{
\item{table_name}{Name of the hypertable}

\item{older_than}{Time interval for chunks to compress}

\item{conn}{Database connection object (optional, uses global connection if NULL)}

\item{debug}{Enable debug messages (optional, default FALSE)}
}
\value{
Number of chunks compressed

TRUE if successful
}
\description{
Manually compress chunks for a hypertable that are older than the specified time.
}
\examples{
\dontrun{
# Initialize connection
init_connection(
  host = "localhost", 
  port = 5432,
  db = "testdb", 
  user = "testuser", 
  pass = "testpass"
)

# Compress chunks older than 7 days
compressed <- compress_chunks("sensor_data", "7 days")

# Or with explicit connection
conn <- get_connection()
compressed <- compress_chunks("sensor_data", "7 days", conn)
}
\dontrun{
# Initialize connection
init_connection(
  host = "localhost", 
  port = 5432, 
  db = "testdb", 
  user = "testuser", 
  pass = "testpass", 
  schema = "public"
)
# Using global connection with required arguments first
compress_chunks("sensor_data", "30 days")

# With explicit connection as optional parameter
conn <- get_connection()
compress_chunks("sensor_data", "30 days", conn)
}
}
