% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tables.R
\name{truncate_table}
\alias{truncate_table}
\title{Truncate a Table}
\usage{
truncate_table(table_name, conn = NULL, schema = NULL, cascade = FALSE)
}
\arguments{
\item{table_name}{Name of the table to truncate}

\item{conn}{Database connection object (optional if global connection is initialized)}

\item{schema}{Schema name (optional)}

\item{cascade}{Whether to also truncate dependent tables}
}
\value{
TRUE if successful
}
\description{
Removes all rows from a table while preserving the table structure.
This operation is faster than DELETE as it:
1. Does not scan the table
2. Does not trigger ON DELETE triggers
3. Does not require VACUUM
4. Does not generate WAL entries for individual row deletions
}
\examples{
\dontrun{
# Truncate a table using global connection
init_connection(
  host = "localhost", 
  port = 5432,
  db = "testdb", 
  user = "testuser", 
  pass = "testpass"
)
truncate_table("my_table")
close_connection()

# Or with explicit connection
conn <- connect_to_db(config)
truncate_table("my_table", conn, cascade = TRUE)
disconnect_from_db(conn)
}
}
