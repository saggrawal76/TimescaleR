% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/package_env.R
\name{has_connection}
\alias{has_connection}
\title{Check if a global connection exists}
\usage{
has_connection(silent = TRUE)
}
\arguments{
\item{silent}{Whether to suppress messages (default: TRUE)}
}
\value{
TRUE if a valid global connection exists, FALSE otherwise
}
\description{
Checks if a global database connection has been initialized and is valid.
}
\examples{
\dontrun{
# Check if a global connection exists
if (has_connection()) {
  # Use existing connection
  results <- get_query("SELECT * FROM my_table")
} else {
  # Initialize a new connection
  init_connection(host = "localhost", port = 5432, db = "testdb", 
                  user = "testuser", pass = "testpass")
}
}
}
